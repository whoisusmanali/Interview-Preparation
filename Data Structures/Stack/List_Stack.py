# using list as stack
stack = []

stack.append(1)
stack.append(2)
stack.append(90)

#Left side of the list is bottom and right side is top
print(stack)
#first value will be like
print(stack[-1])

#length of the Stack
print(len(stack))